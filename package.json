{
  "name": "sphere-player",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/circus2271/sphere-player.git"
  },
  "scripts": {
    "dev": "rimraf ./dist && parcel src/index.html",
    "build": "rimraf ./dist && parcel build src/index.html"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead, iOS 13",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.9.3",
    "parcel": "^2.9.3",
    "rimraf": "^5.0.1"
  },
  "dependencies": {
    "howler": "^2.2.3"
  }
}
