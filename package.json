{
  "name": "sphere-player",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/circus2271/sphere-player.git"
  },
  "scripts": {
    "dev": "npm run clean-up && parcel watch src/index.html --no-hmr & live-server --port=8000 ./dist/",
    "build": "npm run clean-up && parcel build src/index.html",
    "clean-up": "rimraf ./dist && rimraf ./.parcel-cache"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead, iOS 13",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.9.3",
    "live-server": "^1.2.2",
    "parcel": "^2.9.3",
    "rimraf": "^5.0.1"
  },
  "dependencies": {
    "fetch-retry": "^5.0.6",
    "howler": "^2.2.3"
  }
}
